language: cpp
sudo: required
dist: trusty
compiler:
- g++
os:
- linux
script:
- make
- apt-get update && apt-get -y install zip
- zip convertisser *
addons:
  artifacts:
    paths:
    - convertisseur
    - convertisseur.zip
